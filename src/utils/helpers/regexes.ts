
export const alphanumRx              = /^[a-zA-Z0-9]+$/
export const digitStringRx           = /^\d+$/
export const pipeDelimitedNumbersRx  = /^\d+(\|\d+)*$/
export const pipeDelimitedAlphanumRx = /^[a-zA-Z0-9]+(\|[a-zA-Z0-9]+)*$/

export default {
    alphanumRx,
    digitStringRx,
    pipeDelimitedNumbersRx,
    pipeDelimitedAlphanumRx,
}
